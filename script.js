window.addEventListener("load", function (event) {
    const vue = new Vue(
        {
            element:'#vueContainer',
            data: {
                message:  "TITOLO H1",
                classes: "big-font"

            },
            methods: {
                
            }

        }
    );
})